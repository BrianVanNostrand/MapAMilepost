<UserControl x:Class="MapAMilepost.Dockpane1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"   
             xmlns:ui="clr-namespace:MapAMilepost;assembly=MapAMilepost"
             xmlns:viewmodels="clr-namespace:MapAMilepost.ViewModels" 
             xmlns:views="clr-namespace:MapAMilepost.Views"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="364">
  <UserControl.Resources>
        <ResourceDictionary>
            <viewmodels:MainViewModel x:Key="MainViewModel"/>
            <views:MapLineView x:Key="MapLineView"/>
            <views:MapPointView x:Key="MapPointView"/>
            <DataTemplate DataType="{x:Type viewmodels:MapPointViewModel}">
                <ContentControl Content="{StaticResource MapPointView}"/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:MapLineViewModel}">
                <ContentControl Content="{StaticResource MapLineView}"/>
            </DataTemplate>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
  </UserControl.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="50"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
        <DockPanel DataContext="{StaticResource MainViewModel}"  Background="white" Grid.Row="0" LastChildFill="true" KeyboardNavigation.TabNavigation="Local" Height="50">
        <TabControl Height="50" x:Name="controlsGrid">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction Command="{Binding SelectPageCommand}" CommandParameter="{Binding ElementName=controlsGrid}" CommandName="SwitchTabCommand"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <TabItem x:Name="MapPointButton" Height="50" Width="80" Header="Map a Point"/>
                <TabItem x:Name="MapLineButton" Height="50" Width="80" Header="Map a Line"/>
            </TabControl>
    </DockPanel>
        <ScrollViewer DataContext="{StaticResource MainViewModel}" Background="white" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Grid.Row="1">
            <ContentControl DataContext="{StaticResource MainViewModel}" Background="white" ClipToBounds="True" Margin="0,10,0,0" VerticalAlignment="Top" Grid.Row="1" Content="{Binding SelectedViewModel}"/>
        </ScrollViewer>
    </Grid>
</UserControl>